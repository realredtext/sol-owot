var growInterval = 0; var growColor = 0; var tileArray = []; var stuffArray = ["\u29F9","\u239D","\u2572","\u2571","\u239B"]; function randomNumber(min, max) { return Math.round(Math.random() * (min - max) + max) } function randomElement(array) { return array[Math.floor(Math.random() * array.length)] } function grow() { cursorCoordsCurrent = [...randomElement(tileArray), randomNumber(0, 25), randomNumber(0, 15)]; cursorCoords = cursorCoordsCurrent, w.typeChar(randomElement(stuffArray), false, growColor) } menu.addOption("Begin Growing", function() { growInterval = setInterval(grow, 1); tileArray = getVisibleTiles(); }); menu.addOption("Stop Growing", function() { clearInterval(growInterval); }); menu.addOption("Configure Growth", function() { stuffArray = prompt("What characters to place randomly on the screen? Do not separate with spaces or commas, just type the characters in.", "\u29F9\u239D\u2572\u2571\u239B").split(""); growColor = String(parseInt(prompt("Hex color code for the characters","08FF34"), 16)) });
